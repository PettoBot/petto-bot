const { createRoleplayCommand } = require('../../utils/roleplay');
module.exports = createRoleplayCommand('glomp', { label: 'glomp', verb: 'glomps', description: 'Launch a dramatic friendly hug.' });
