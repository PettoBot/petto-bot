const { createRoleplayCommand } = require('../../utils/roleplay');
module.exports = createRoleplayCommand('kiss', { label: 'kiss', verb: 'kisses', description: 'Give someone a sweet, non-explicit kiss.' });
