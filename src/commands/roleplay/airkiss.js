const { createRoleplayCommand } = require('../../utils/roleplay');
module.exports = createRoleplayCommand('airkiss', { label: 'air kiss', verb: 'blows an air kiss to', self: '**{actor}** blows an air kiss into the room.', description: 'Blow a playful air kiss.' });
